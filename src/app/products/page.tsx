import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Produtos',
}

export default function Products() {
    return (
        <div>
            <h1>Products</h1>
        </div>
    );
}